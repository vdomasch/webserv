GET:
	- problem no icon on webpage
	- return appropriate error_page, function already there but header settings to review:
		405: Method not allowed -> need to check which one is allowed for specified path
	- check system error like stat()
	- secure path (reject unauthorized path request)

	! After that I think it's done !

POST:
	to do

DELETE:
	to do
	
TO DO:

	Quand declare dans deux partie server differentes, un meme port+servername sont acceptes, erreur ?
	⮑ verifier les erreurs